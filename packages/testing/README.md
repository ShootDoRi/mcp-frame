# @mcp-frame/testing

Local testing utilities for MCP app frames.

This package includes `createMockMcpHost()` and fixtures for exercising
postMessage bridge flows in unit tests and local examples.

See the repository README and `docs/testing.md` for usage.
