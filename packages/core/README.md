# @mcp-frame/core

Core JSON-RPC over postMessage utilities for MCP app frames.

This package provides the import-safe bridge abstraction, MCP frame message
types, JSON-RPC helpers, and runtime validation helpers used by mcp-frame.

See the repository README and `docs/bridge.md` for usage and security notes.
