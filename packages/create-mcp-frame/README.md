# create-mcp-frame

Minimal scaffolding CLI for Vite + React MCP app frames.

Run `npx create-mcp-frame my-app` to generate a small React app that uses
`@mcp-frame/react`.

See the repository README for project status and roadmap.
