# @mcp-frame/react

React provider and hooks for iframe-based MCP app frames.

The main public API is intentionally small: `McpFrameProvider`, frame hooks, and
`FrameErrorBoundary`. Debug UI helpers are available from
`@mcp-frame/react/debug`.

See the repository README and `docs/getting-started.md` for usage.
